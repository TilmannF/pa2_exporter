[project]
name = "pa2_exporter"
# Read back from pa2_exporter.__version__ so the running file is the single
# source of truth; importlib.metadata would otherwise report whatever is
# installed, which is not necessarily what is executing.
dynamic = ["version"]
description = "Prometheus exporter for the dbx DriveRack PA2 loudspeaker management system"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = [
    "prometheus-client>=0.19",
]

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "ruff>=0.6",
]

[project.scripts]
pa2-exporter = "pa2_exporter:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["pa2_exporter", "pa2_poc"]

[tool.setuptools.dynamic]
version = {attr = "pa2_exporter.__version__"}

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py39"
exclude = [".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "BLE", "FURB", "EXE"]
